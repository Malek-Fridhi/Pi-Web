{% extends 'base.html.twig' %}

{% block title %}Depense{% endblock %}

{% block body %}
    <h1>Depense</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Iddepense</th>
                <td>{{ depense.iddepense }}</td>
            </tr>
            <tr>
                <th>TypeDep</th>
                <td>{{ depense.typeDep }}</td>
            </tr>
            <tr>
                <th>MontantDep</th>
                <td>{{ depense.montantDep }}</td>
            </tr>
            <tr>
                <th>DatereceptionDep</th>
                <td>{{ depense.datereceptionDep ? depense.datereceptionDep|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_depense_index') }}">back to list</a>

    <a href="{{ path('app_depense_edit', {'iddepense': depense.iddepense}) }}">edit</a>

    {{ include('depense/_delete_form.html.twig') }}
{% endblock %}








