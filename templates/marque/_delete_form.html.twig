<form method="post" action="{{ path('app_marque_delete', {'id_marque': marque.id_marque}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ marque.id_marque) }}">
    <button class="btn">Delete</button>
</form>
