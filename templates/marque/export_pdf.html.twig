<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des Marques - GeekHub</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        .header { text-align: center; margin-bottom: 30px; border-bottom: 2px solid #3498db; padding-bottom: 10px; }
        .header h1 { color: #2c3e50; margin-bottom: 5px; }
        .header p { color: #7f8c8d; margin-top: 0; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th { background-color: #3498db; color: white; padding: 10px; text-align: left; }
        td { padding: 8px; border: 1px solid #ddd; }
        tr:nth-child(even) { background-color: #f8f9fa; }
        .footer { margin-top: 30px; text-align: right; font-size: 0.8em; color: #6c757d; border-top: 1px solid #dee2e6; padding-top: 10px; }
        .logo-img { max-width: 80px; max-height: 40px; }
        .text-center { text-align: center; }
        .badge {
            padding: 3px 8px;
            border-radius: 10px;
            font-size: 0.8em;
            font-weight: bold;
            color: white;
        }
        .badge-success { background-color: #28a745; }
        .badge-warning { background-color: #ffc107; }
        .badge-danger { background-color: #dc3545; }
    </style>
</head>
<body>
    <div class="header">
        <h1>Liste complète des Marques</h1>
        <p>Centre Sportif GeekHub</p>
    </div>

    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Nom</th>
                <th>Logo</th>
                <th>Téléphone</th>
                <th>Email</th>
            </tr>
        </thead>
        <tbody>
            {% for marque in marques %}
            <tr>
                <td>{{ marque.idMarque }}</td>
                <td>{{ marque.nom }}</td>
                <td class="text-center">
                    {% if marque.image %}
                        <img src="{{ app.request.schemeAndHttpHost ~ '/uploads/marques/' ~ marque.image }}" 
                             alt="{{ marque.nom }}" 
                             class="logo-img">
                    {% else %}
                        <span>-</span>
                    {% endif %}
                </td>
                <td>{{ marque.telephone }}</td>
                <td>{{ marque.email }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="footer">
        <p>Total marques : {{ marques|length }}</p>
        <p>Généré le {{ date|date('d/m/Y à H:i') }}</p>
    </div>
</body>
</html>