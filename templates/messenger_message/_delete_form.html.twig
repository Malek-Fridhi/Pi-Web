<form method="post" action="{{ path('app_messenger_message_delete', {'id': messenger_message.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ messenger_message.id) }}">
    <button class="btn">Delete</button>
</form>
