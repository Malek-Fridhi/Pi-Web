<form method="post" action="{{ path('app_equipement_delete', {'id_equipement': equipement.id_equipement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipement.id_equipement) }}">
    <button class="btn">Delete</button>
</form>
